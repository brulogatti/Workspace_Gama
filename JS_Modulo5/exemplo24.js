//Crie um algoritmo que converte dias em horas, quando recebe um número de dias.
function converterdias(dias){
    return dias*24;
}

var dias = 45;
console.log("%s dias em horas são: %s", dias, converterdias(dias));