let gql;_1e5‍.x([["typeDefs",()=>typeDefs],["ListSortmentEnum",()=>ListSortmentEnum],["resolvers",()=>resolvers]]);_1e5‍.w("apollo-server-express",[["gql",["gql"],function(v){gql=v}]]);

       const typeDefs = gql`
    interface List {
        items: [Node!]!
        totalItems: Int!
    }

    enum ListSortmentEnum{
        ASC
        DESC
    }

    input ListSort{
        sorter: String!
        sortment: ListSortmentEnum!
    }
`;

       const ListSortmentEnum = Object.freeze({
    ASC: 'ASC',
    DESC: 'DESC',
});

       const resolvers = {
    List: {
        __resolveType: () => null,
    },
};